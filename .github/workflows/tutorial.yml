name: tutorial
on: [push]
jobs:
  build:
    name: build
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: build
        run: |
          node -v
          npm -v
          npm install
          npm run build
